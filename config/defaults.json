{
  "reviewer": [
    "gemini",
    "opus",
    "deepseek-flash",
    "muse-spark"
  ],
  "note": "Default reviewer(s) for a bare /era. A list is dispatched simultaneously as a panel. 4-model panel since 2026-08-22. The 4th seat was `ox-alpha` (free); on 2026-08-26 the operator swapped it for `muse-spark` (then opencode-go/muse-spark-1.2-contributor). On 2026-09-04 that preset was bumped to opencode-go/muse-spark-1.3-contributor, display name exactly 'Muse Spark 1.3 Contributor' -- same price and same context/output limits, verified from `opencode models --verbose` that day. That seat is no longer free — $0.10/$0.20 per Mtok, ~$0.01 on a 100k-token round — cheaper than deepseek-flash and immaterial next to opus, but the original \"costs nothing\" argument for a 4th reviewer no longer applies; the redundancy argument still does. `ox-alpha` was DELETED from the registry on 2026-09-05: `retired` is read by nothing at runtime, so it still dispatched and failed at the vendor rather than at era. The gemini seat bumped 3.6 -> 3.8 the same day (pricing inherited, not measured). MUST stay in lockstep with runtimes/_era-defaults.ps1 $EraShippedPanel. Edit via `/era set default <names>` or by hand."
}
